{
  "name": "igreja-contabilidade",
  "version": "1.0.0",
  "description": "Sistema de contabilidade para igreja",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "start": "cd client && npm start",
    "dev": "cd client && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}